@page "/components/file-explorer"
@using System.Reflection

<PageTitle>File Explorer | Shift Framework</PageTitle>

<MudContainer Class="mt-15">
    <MudText Typo="Typo.h4">File Explorer</MudText>

</MudContainer>

<MudContainer Class="mt-15" MaxWidth="MaxWidth.False" Style="float: left;">

    <MudText Typo="Typo.h6">Before You Begin</MudText>
    <MudPaper Elevation="1" Class="mt-1 pa-2">
        <MudText Typo="Typo.body1" Class="mb-2">
            The <strong>FileExplorer</strong> component relies on <strong>Azure Storage Blobs</strong>. Before using it, you must define the required storage accounts and containers in your application configuration
        </MudText>

        <MudText Typo="Typo.body1" Class="mb-2">
            During development, you can use <strong>Azurite</strong> to run a local storage emulator. This allows you to test the FileExplorer component without needing an actual Azure Storage account.
        </MudText>

        <MudText Typo="Typo.body1" Class="mb-2">
            To set this up, go to your projects's <strong>Connected Services</strong> page, add the <em>Storage Azurite emulator (local)</em> service, and use the following connection string:
            <MudText Typo="Typo.subtitle2" Class="mt-1">Connection String: <code>UseDevelopmentStorage=true</code></MudText>
        </MudText>

        <MudText Typo="Typo.body1" Class="mb-2">
            Below is a sample configuration for development:
        </MudText>

        <textarea class="mt-2 mb-2" style="width: 100%; height: 250px; resize: none;  border: 1px solid @Colors.Blue.Lighten1;" readonly>
"AzureStorageAccounts": [
    {
        "AccountName": "devstoreaccount1",
        "AccountKey": "Eby8vdM02xNOcqFlqUwJPLlmEtlCDXJ1OUzFT50uSRZ6IFsuFq2UVErCz4I6tq/K1SZFPTOtr/KBHBeksoGMGw==",
        "ConnectionString": "UseDevelopmentStorage=true",
        "DefaultContainerName": "development",
        "EndPoint": "http://127.0.0.1:10000/devstoreaccount1",
        "SupportsFileExplorer": true,
        "IsDefaultAccount": true
    }
]</textarea>
    
    </MudPaper>

    <MudText Typo="Typo.h6">Basic Usage</MudText>
    <MudPaper Elevation="1" Class="mt-1">
        <div class="d-flex">
            <MudSpacer />
            <MudButton Variant="Variant.Text" Color="Color.Warning" StartIcon="@Icons.Material.Filled.Code">
                Code
            </MudButton>
        </div>

        <div class="pa-5">

            <FileExplorer></FileExplorer>
        </div>
    </MudPaper>
</MudContainer>
